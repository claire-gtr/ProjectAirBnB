
    <div class="container">
      <h2>Annonces</h2>
        <div <class="row">
          <% @cars.each do |car| %>
             <h4><%= car.name %></h4>
             <h6><%= car.description %></h6>
             <h6><%= car.model %></h6>
              <%=link_to "See more", car_path(car) %>
           <% end %>
        </div>
    </div>


<% if user_signed_in? %>
<%= link_to "Add a new car", new_car_path %>
<% end %>
