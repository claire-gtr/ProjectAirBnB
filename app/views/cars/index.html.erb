<h2>Annonces</h2>

    <div class="container">
        <div <class="row">
          <% @cars.each do |car| %>
             <h4><%= car.name %></h4>
             <h6><%= car.description %></h6>
             <h6><%= car.model %></h6>
              <%=link_to "See more", car_path(car) %>
           <% end %>
        </div>
    </div>
